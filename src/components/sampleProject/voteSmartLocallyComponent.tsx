import * as React from "react";


const VoteSmartLocallyComponent = ({},{}) => {

    return (
        <div>
            vote smart kids
        </div>
    );

}

export default VoteSmartLocallyComponent;
